# Base URL of the DisneyWaits backend API.
# Copy this file to .env.local and adjust if your backend runs elsewhere.
# Defaults to http://localhost:8080 if unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
